import { escapeHTML } from "../constants.js";
export const lobbyView=(nickname,message)=>`<h1 class="brand">🎭 LIAR GAME</h1>${message?`<p class="card error">${escapeHTML(message)}</p>`:""}<section class="card stack"><div><span class="muted">닉네임</span><h2>${escapeHTML(nickname)}</h2></div><form data-action="create" class="stack"><button>새 방 만들기</button></form><form data-action="join" class="stack"><label for="room-code">방 코드로 참가</label><input id="room-code" name="code" maxlength="6" pattern="[A-Za-z0-9]{6}" placeholder="ABC123" required><button>참가하기</button></form><button class="secondary" data-action="change-nickname">닉네임 변경</button></section>`;
