import { escapeHTML } from "../constants.js";
export function speakingView(s,isHost){const index=s.round.current_speaker_index;const current=s.round_players.find(p=>p.turn_order===index);const last=index===s.round_players.length-1;return `<section class="card stack"><h2>발언 순서</h2><div class="speaker"><span>${index+1} / ${s.round_players.length}</span><strong>${escapeHTML(current?.nickname_snapshot||"-")}</strong></div><ol>${s.round_players.map(p=>`<li>${p.turn_order+1}. ${escapeHTML(p.nickname_snapshot)}</li>`).join("")}</ol>${isHost?`<div class="row"><button class="secondary" data-action="speaker-prev" ${index===0?"disabled":""}>PREVIOUS</button>${last?`<button data-action="finish-speaking">발언 종료 (방장)</button>`:`<button data-action="speaker-next">NEXT (방장)</button>`}</div>`:""}</section>`;}
export const discussionView=()=>`<section class="card stack"><h2>자유 토론</h2><p class="notice">투표 기능은 다음 구현 단계에서 제공됩니다.</p></section>`;
