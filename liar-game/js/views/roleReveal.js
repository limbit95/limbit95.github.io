import { escapeHTML } from "../constants.js";
export function roleRevealView(s,role){return `<section class="card stack"><h2>역할 확인</h2>${role?`<p class="muted">카테고리: ${escapeHTML(role.category)}</p>${role.role==="liar"?`<p class="role-word">당신은 라이어입니다.</p>`:`<p class="role-word">${escapeHTML(role.word)}</p>`}<button data-action="confirm-role">확인했습니다</button>`:`<p>역할 보기 버튼은 본인의 역할 전용 RPC를 호출합니다.</p><button data-action="show-role">내 역할 보기</button>`}<p class="muted">확인 완료 ${s.round_players.filter(p=>p.role_checked).length}/${s.round_players.length}</p><button data-action="start-speaking">발언 시작 (방장)</button></section>`;}
